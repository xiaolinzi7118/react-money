type RecordItem = {
    id: string,
    type: 'out' | 'in',
    account: number,
    tagId: number,
    note: string,
    createdAt: string
}
const useRecordList = () => {

}
export { useRecordList }